#include <iostream>     // 標準入出力
#include <vector>       // ベクター
#include <algorithm>    // ソートやその他のアルゴリズム
#include <cmath>        // 数学関数
#include <string>       // 文字列処理
#include <map>          // マップ
#include <set>          // セット
#include <queue>        // キュー、優先度キュー
#include <stack>        // スタック
#include <tuple>        // タプル
#include <iomanip>      // 出力のフォーマット指定
#include <limits>       // 数値の限界値
#include <numeric> // accumulateを使うために必要
#define rep(i, start, end) for (int i = (start); i < (end); ++i)
using namespace std;


int main(){

}
